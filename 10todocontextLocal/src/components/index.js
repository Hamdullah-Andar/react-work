import TodoForm from "./TodoForm";
import TodoItem from "./TodoItem";

export { TodoForm, TodoItem };

// index file is normally used to export all the components from a single file, so that we can import them easily in other files.
// we import all the components from the components folder in the index.js file and then export them.
